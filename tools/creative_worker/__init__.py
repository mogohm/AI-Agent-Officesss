"""Local Claude-operated creative image worker (Phase 1).

A small, offline CLI that turns a creative task JSON into a generated image saved
at an exact project asset path. No server, no queue — just a script Claude Code
runs from VSCode. See docs/CREATIVE_WORKER.md.
"""
