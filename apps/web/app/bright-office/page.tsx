"use client";
// Bright Office PREVIEW route — same production component, internal company
// selection (no route param). Kept for visual review and lab comparisons.
import { BrightCompanyOffice } from "@/components/bright/BrightCompanyOffice";

export default function BrightOfficePreviewPage() {
  return <BrightCompanyOffice />;
}
